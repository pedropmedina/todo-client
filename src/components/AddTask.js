import React from 'react';

const style = {
	backgroundColor: 'red',
	position: 'absolute',
	top: '30px',
	bottom: 0,
	left: 0,
	right: 0,
};

const AddTask = () => <div>Add Task</div>;

export default AddTask;
